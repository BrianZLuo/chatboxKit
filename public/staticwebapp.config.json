{
  "routes": [
    {
      "route": "/*",
      "serve": "/index.html",
      "statusCode": 200
    }
  ],
  "build": {
    "command": "npm run build",
    "outputPath": "build"
  },
  "appArtifactLocation": "build",
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}